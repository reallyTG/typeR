library(BigSEM)


### Name: getedges
### Title: Retrieve All the Edges from the Fitted Systems of Structural
###   Equations
### Aliases: getedges

### ** Examples

data(simdata)
attach(simdata)
#simsem <- fitsem(y=y,x=x,sk=sk)

#edges <- getedges(simsem)

# Check the first connect
#cat(simsem$y.names[edges$target[1]], '<-', simsem$y.names[edges$source[1]],'\n')



