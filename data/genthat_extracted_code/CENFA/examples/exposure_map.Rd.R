library(CENFA)


### Name: exposure_map
### Title: Create an exposure map
### Aliases: exposure_map

### ** Examples

dep <- departure(x = climdat.hist, y = climdat.fut, s.dat = ABPR)
exp.map <- exposure_map(dep)




