library(IPtoCountry)


### Name: IP_location
### Title: Convert IP address to location (country name, region, city, zip
###   code, latitude, longitude, and GMT)
### Aliases: IP_location

### ** Examples

# Only run this example in interactive R sessions
if(interactive()) {
  IP.location = IP_location(IPs)
  head(IP.location)
}



