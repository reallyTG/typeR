library(MSG)


### Name: cut_plot
### Title: Cut the points in a scatter plot into groups according to x-axis
### Aliases: cut_plot

### ** Examples

x = rnorm(100)
y = rnorm(100)
cut_plot(x, y, seq(min(x), max(x), length = 5))



