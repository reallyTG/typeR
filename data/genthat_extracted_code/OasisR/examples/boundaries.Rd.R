library(OasisR)


### Name: boundaries
### Title: A function to compute the matrix of common boundaries
### Aliases: boundaries

### ** Examples

 boundaries(segdata) 
foldername <- system.file('extdata', package = 'OasisR')
shapename <- 'segdata'
boundaries(folder = foldername, shape = shapename)



