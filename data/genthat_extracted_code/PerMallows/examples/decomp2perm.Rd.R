library(PerMallows)


### Name: decomp2perm
### Title: Get a permutation consistent with a decomposition vector
### Aliases: decomp2perm

### ** Examples

decomp2perm(c(1,0,1,0,0), "kendall")
decomp2perm(c(1,0,1,0,0), "cayley")
decomp2perm(c(1,0,1,0,0), "hamming")



