library(RFmarkerDetector)


### Name: plot.pca.scores
### Title: PCA Scores plot This function creates a plot that graphically
###   projects the original samples onto the subspce spanned by the first
###   two principal components
### Aliases: plot.pca.scores

### ** Examples

## data(cachexiaData)
## pca_obj <- pca(cachexiaData, autoscale = TRUE, exclude = TRUE) 
## plot.pca.scores(pca_obj, cachexiaData)



