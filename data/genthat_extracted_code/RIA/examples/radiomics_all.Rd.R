library(RIA)


### Name: radiomics_all
### Title: Calculates all radiomic statistics on supplied RIA_image
### Aliases: radiomics_all

### ** Examples

## Not run: 
##D #Discretize loaded image and then calculate all radiomic statistics
##D RIA_image <- radiomics_all(RIA_image, equal_prob = FALSE, bins_in= c(2,8,32,128), distance = c(1:3))
##D RIA_image <- radiomics_all(RIA_image, equal_prob = TRUE, bins_in= c(2,8,32,128), distance = c(1:3))
## End(Not run)




