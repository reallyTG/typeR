library(RSNNS)


### Name: encodeClassLabels
### Title: Encode a matrix of (decoded) class labels
### Aliases: encodeClassLabels

### ** Examples

data(iris)
labels <- decodeClassLabels(iris[,5])
encodeClassLabels(labels)



