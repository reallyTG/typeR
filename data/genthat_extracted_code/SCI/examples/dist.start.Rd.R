library(SCI)


### Name: dist.start
### Title: Rough estimates for parameters of selected distributions
### Aliases: dist.start lmom.start mom.start

### ** Examples

lmom.start(rgamma(100,shape=0.5,rate=1),"gamma")

mom.start(rgamma(100,shape=0.5,rate=1),"gamma")

dist.start(rgamma(100,shape=0.5,rate=1),"gamma")



