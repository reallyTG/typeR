library(SLIDE)


### Name: bootstrap_cutoff
### Title: Bootstrap Cutoff for SLIDE
### Aliases: bootstrap_cutoff

### ** Examples

cutoff <- bootstrap_cutoff(uninfected = UN_sig, fraction = (nrow(I_sig)/nrow(UN_sig)), iter=1)
cutoff



