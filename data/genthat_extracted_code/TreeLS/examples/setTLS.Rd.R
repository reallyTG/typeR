library(TreeLS)


### Name: setTLS
### Title: Reset or create a 'LAS' object depending on the input's type
### Aliases: setTLS

### ** Examples

cld = matrix(runif(300, 0, 10), ncol=3)
cld = setTLS(cld)
summary(cld)



