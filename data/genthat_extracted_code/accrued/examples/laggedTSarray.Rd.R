library(accrued)


### Name: laggedTSarray
### Title: Function to create an array of time series across accrual lags
###   using the running median
### Aliases: laggedTSarray

### ** Examples

	data(accruedDataExample)			# simulated accrued data
	dat <- data.accrued(accruedDataExample)	# Convert to data.accrued object
	laggedTSarray(dat)



