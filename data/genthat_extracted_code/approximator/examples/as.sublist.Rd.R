library(approximator)


### Name: as.sublist
### Title: Converts a level one design matrix and a subsets object into a
###   list of design matrices, one for each level
### Aliases: as.sublist
### Keywords: array

### ** Examples

data(toyapps)
as.sublist(D1=D1.toy , subsets=subsets.toy)



