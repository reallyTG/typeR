library(assertive.types)


### Name: assert_is_factor
### Title: Is the input a factor?
### Aliases: assert_is_factor assert_is_ordered is_factor is_ordered

### ** Examples

assert_is_factor(factor(sample(letters, 10)))



