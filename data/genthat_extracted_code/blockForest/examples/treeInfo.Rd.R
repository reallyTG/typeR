library(blockForest)


### Name: treeInfo
### Title: Tree information in human readable format
### Aliases: treeInfo

### ** Examples

require(blockForest)
rf <- blockForest(Species ~ ., data = iris)
treeInfo(rf, 1)



