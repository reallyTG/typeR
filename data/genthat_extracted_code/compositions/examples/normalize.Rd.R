library(compositions)


### Name: normalize
### Title: Normalize vectors to norm 1
### Aliases: normalize normalize.default
### Keywords: multivariate

### ** Examples

data(SimulatedAmounts)
normalize(c(1,2,3))
normalize(acomp(c(1,2,3)))
norm(normalize(acomp(sa.groups)))



