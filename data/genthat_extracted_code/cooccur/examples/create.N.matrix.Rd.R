library(cooccur)


### Name: create.N.matrix
### Title: Function to create spp x spp matrix of potential co-occurring
###   sites (N) from a potential spp x site matrix.
### Aliases: create.N.matrix
### Keywords: co-occurrence

### ** Examples

#data(finches)
#N_matrix <- matrix(data = rbinom(n = nrow(finches)*ncol(finches),1,prob = 0.75),
#                        nrow = nrow(finches),
#                        ncol = ncol(finches)
#                        ,byrow = T)
#create.N.matrix(N_matrix)



