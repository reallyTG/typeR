library(ctmle)


### Name: bound
### Title: set outliers to min/max allowable values. It assumes x contains
###   only numerical data
### Aliases: bound

### ** Examples

x <- rnorm(1000)
x <- bound(x, c(-1, 1))



