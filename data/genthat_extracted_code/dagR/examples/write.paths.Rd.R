library(dagR)


### Name: write.paths
### Title: Write the paths into a DAG drawing.
### Aliases: write.paths

### ** Examples

dag1<-demo.dag1();
dag.draw(dag1);
dag1a<-dag.adjust(dag1);
write.paths(dag1a);



