library(dupiR)


### Name: Counts
### Title: Class "Counts" - a container for measurements and dupiR
###   inference results
### Aliases: Counts Counts-class summary,Counts-method
### Keywords: class

### ** Examples

# create an object of class 'Counts' by using new
new('Counts', counts = c(30, 35), fractions = c(0.075, 0.1))

#or by means of the constructor
newCounts(counts = c(30, 35), fractions = c(0.075, 0.1))



