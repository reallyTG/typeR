library(epiphy)


### Name: mapped_var
### Title: Existing variable mappings.
### Aliases: mapped_var mapped_var<-

### ** Examples

my_data <- count(aphids)
my_data
mapped_var(my_data)
mapped_var(my_data) <- mapping(x = X, y = Y)
mapped_var(my_data)
mapped_var(my_data) <- mapping(x = x, r = r, keep = FALSE)
mapped_var(my_data)




