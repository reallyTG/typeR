library(equaltestMI)


### Name: eqMI.RMSEA
### Title: Obtain the RMSEA cutoff values for equivalence testing
### Aliases: eqMI.RMSEA

### ** Examples

alpha <- .05;
N <- 200;
m <- 1;
T_ml <- 28.446; #the statistic T_ml for group 1;
df <- 24;
eqMI.RMSEA(N = N, m = m, df = df);




