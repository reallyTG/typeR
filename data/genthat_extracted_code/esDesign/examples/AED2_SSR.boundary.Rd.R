library(esDesign)


### Name: AED2_SSR.boundary
### Title: Calculate the futility and efficacy stopping boundaries of the
###   Adaptive Enrichment Design (Strategy 2) with Sample Size
###   Re-estimation Procedure
### Aliases: AED2_SSR.boundary

### ** Examples

rho <- 0.5
alpha <- 0.05
pstar <- 0.15
epsilon <- 0.5
AED2_SSR.boundary(rho = rho, alpha = alpha, pstar = pstar, epsilon = epsilon)



