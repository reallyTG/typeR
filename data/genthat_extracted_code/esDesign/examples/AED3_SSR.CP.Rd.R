library(esDesign)


### Name: AED3_SSR.CP
### Title: Calculate the N2 and the critical value C in the Adaptive
###   Enrichment Design (Strategy 3) with Sample Size Re-estimation
###   Procedure
### Aliases: AED3_SSR.CP

### ** Examples

Z1 <- 1.974
delta <- 0.355
N1 <- 248
pstar <- 0.15
alpha <- 0.05
rho <- 0.5
beta <- 0.20
N2 <- 108
AED3_SSR.CP(Z1 = Z1, delta = delta, N1 = N1, pstar = pstar,
           alpha = alpha, rho = rho, beta = beta, N2 = N2)




