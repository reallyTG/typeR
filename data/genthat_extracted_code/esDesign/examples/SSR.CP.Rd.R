library(esDesign)


### Name: SSR.CP
### Title: Calculate the N2 and the critical value C in Sample Size
###   Re-estimation Procedure
### Aliases: SSR.CP

### ** Examples

Z1 <- 1.527
delta <- 0.137
N1 <- 248
pstar <- 0.15
alpha <- 0.05
beta <- 0.2
res <- SSR.CP(Z1 = Z1, delta = delta, N1 = N1,
       pstar = pstar, alpha = alpha, beta = beta)




