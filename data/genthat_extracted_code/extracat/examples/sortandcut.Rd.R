library(extracat)


### Name: sortandcut
### Title: Sort-and-Cut Reordering
### Aliases: sortandcut

### ** Examples

M <- arsim(12000,c(30,40),7,noise=0.3)
c1 <- cfluctile(M1<-optile(M, iter = 20))
c2 <- cfluctile(M2<-sortandcut(M))




