library(filenamer)


### Name: make_path
### Title: Create directory structure for a file path
### Aliases: make_path make_path.character make_path.filename

### ** Examples

## Not run: 
##D # CRAN policy forbids package example to write to current directory,
##D # even inside \dontrun because the user may copy-and-paste and
##D # polute his/her current directory;
##D # in real-world setting, the `tempdir` path prefix is unnecessary
##D x <- file.path(tempdir(), "path/to/file.txt")
##D 
##D fn <- as.filename(x)
##D make_path(fn)
## End(Not run)



