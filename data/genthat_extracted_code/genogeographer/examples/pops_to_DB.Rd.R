library(genogeographer)


### Name: pops_to_DB
### Title: Pre-compute the scores for a given reference database
### Aliases: pops_to_DB

### ** Examples

df_ <- simulate_pops(pop_n = 4, aims_n = 50)
df_db <- pops_to_DB(df_)



