library(hctrial)


### Name: strat_start
### Title: Initializes a subspace stratified design before the start of the
###   study.
### Aliases: strat_start

### ** Examples

p0_sub <- c(0.1, 0.3, 0.5)
p1_sub <- c(0.3, 0.5, 0.7)
distr_sub <- c(1/3, 1/3, 1/3)
strat_start(p0_sub, p1_sub, distr_sub)



