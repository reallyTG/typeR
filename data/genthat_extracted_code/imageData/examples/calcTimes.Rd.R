library(imageData)


### Name: calcTimes
### Title: Calculates for a set of times, the time intervals after an
###   origin time and the position of each with in that time
### Aliases: calcTimes
### Keywords: manip

### ** Examples

  data(exampleData)
  raw.dat <- calcTimes(data = raw.dat, 
                       imageTimes = "Snapshot.Time.Stamp", timePositions = "Hour")



