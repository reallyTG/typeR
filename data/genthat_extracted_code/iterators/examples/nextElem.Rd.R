library(iterators)


### Name: nextElem
### Title: Get Next Element of Iterator
### Aliases: nextElem nextElem.containeriter nextElem.funiter
### Keywords: methods

### ** Examples

  it <- iter(c('a', 'b', 'c'))
  print(nextElem(it))
  print(nextElem(it))
  print(nextElem(it))



