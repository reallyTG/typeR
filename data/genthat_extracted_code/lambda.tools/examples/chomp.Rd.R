library(lambda.tools)


### Name: chomp
### Title: Remove the head and tail of a data structure
### Aliases: chomp

### ** Examples

chomp(1:10)
chomp(letters)

chomp(data.frame(x=1:10, y=1:10), head=2, tail=2)



