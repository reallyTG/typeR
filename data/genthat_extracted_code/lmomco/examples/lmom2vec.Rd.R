library(lmomco)


### Name: lmom2vec
### Title: Convert an L-moment object to a Vector of L-moments
### Aliases: lmom2vec
### Keywords: utility (vector) L-moment (utility) utility (L-moments to
###   vector of)

### ** Examples

  lmr <- lmoms(rnorm(40))
  lmom2vec(lmr)
  lmr <- vec2lmom(c(140,150,.3,.2,-.1))
  lmom2vec(lmr)



