library(logger)


### Name: colorize_by_log_level
### Title: Colorize string by the related log level
### Aliases: colorize_by_log_level

### ** Examples

## Not run: 
##D cat(colorize_by_log_level(FATAL, 'foobar'), '\n')
##D cat(colorize_by_log_level(ERROR, 'foobar'), '\n')
##D cat(colorize_by_log_level(WARN, 'foobar'), '\n')
##D cat(colorize_by_log_level(SUCCESS, 'foobar'), '\n')
##D cat(colorize_by_log_level(INFO, 'foobar'), '\n')
##D cat(colorize_by_log_level(DEBUG, 'foobar'), '\n')
##D cat(colorize_by_log_level(TRACE, 'foobar'), '\n')
## End(Not run)



