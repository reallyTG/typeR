library(logger)


### Name: grayscale_by_log_level
### Title: Render a string with light/dark gray based on the related log
###   level
### Aliases: grayscale_by_log_level

### ** Examples

## Not run: 
##D cat(grayscale_by_log_level(FATAL, 'foobar'), '\n')
##D cat(grayscale_by_log_level(ERROR, 'foobar'), '\n')
##D cat(grayscale_by_log_level(WARN, 'foobar'), '\n')
##D cat(grayscale_by_log_level(SUCCESS, 'foobar'), '\n')
##D cat(grayscale_by_log_level(INFO, 'foobar'), '\n')
##D cat(grayscale_by_log_level(DEBUG, 'foobar'), '\n')
##D cat(grayscale_by_log_level(TRACE, 'foobar'), '\n')
## End(Not run)



