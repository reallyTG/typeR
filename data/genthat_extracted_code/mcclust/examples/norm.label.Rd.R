library(mcclust)


### Name: norm.label
### Title: Norm Labelling of a Clustering
### Aliases: norm.label
### Keywords: cluster

### ** Examples

(cl <- sample(c(13,12,34), 13, replace=TRUE))
norm.label(cl)

(cl <- sample(c("a","b","f31"), 13, replace=TRUE))
norm.label(cl)



