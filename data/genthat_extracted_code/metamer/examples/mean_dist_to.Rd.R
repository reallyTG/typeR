library(metamer)


### Name: mean_dist_to
### Title: Mean minimum distance
### Aliases: mean_dist_to

### ** Examples

target <- data.frame(x = rnorm(100), y = rnorm(100))
data <- data.frame(x = rnorm(100), y = rnorm(100))
distance <- mean_dist_to(target)
distance(data)




