library(mregions)


### Name: mr_geo_code
### Title: Geocode with Marineregions
### Aliases: mr_geo_code

### ** Examples

## Not run: 
##D # search for 'oost', like=TRUE, and not fuzzy
##D mr_geo_code(place = "oost", like = TRUE, fuzzy = FALSE)
##D 
##D # search for 'oost', like=FALSE, and not fuzzy
##D mr_geo_code(place = "oost", like = FALSE, fuzzy = FALSE)
##D 
##D # search for 'oost', like=FALSE, and fuzzy
##D mr_geo_code(place = "oost", like = FALSE, fuzzy = TRUE)
##D 
##D # search for 'oost', like=TRUE, and fuzzy
##D mr_geo_code(place = "oost", like = TRUE, fuzzy = TRUE)
##D 
##D # search for 'ast', like=TRUE, and fuzzy
##D mr_geo_code(place = "ast", like = TRUE, fuzzy = TRUE)
## End(Not run)



