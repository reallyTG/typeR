library(normalr)


### Name: normaliseData
### Title: Apply normalisation on a data frame using specific Lambda value
### Aliases: normaliseData

### ** Examples

## Not run: normaliseData(mtcars, getLambda(mtcars, parallel = FALSE))
normaliseData(mtcars, getLambda(mtcars, parallel = FALSE))



