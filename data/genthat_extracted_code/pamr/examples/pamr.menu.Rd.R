library(pamr)


### Name: pamr.menu
### Title: A function that interactively leads the user through a PAM
###   analysis
### Aliases: pamr.menu

### ** Examples

set.seed(120)
x <- matrix(rnorm(1000*20),ncol=20)
y <- sample(c(1:4),size=20,replace=TRUE)
mydata <- list(x=x,y=y)
#  pamr.menu(mydata)



