library(parsec)


### Name: minimal
### Title: Minimal elements of a poset
### Aliases: minimal minimal.cover minimal.incidence

### ** Examples

vl <- c(3, 2, 4)
prof <- var2prof(varlen = vl)
Z <- getzeta(prof)

minimal(Z)



