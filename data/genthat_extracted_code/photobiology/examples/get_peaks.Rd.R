library(photobiology)


### Name: get_peaks
### Title: Get peaks and valleys in a spectrum
### Aliases: get_peaks get_valleys

### ** Examples

with(sun.spct, get_peaks(w.length, s.e.irrad))
with(sun.spct, get_valleys(w.length, s.e.irrad))




