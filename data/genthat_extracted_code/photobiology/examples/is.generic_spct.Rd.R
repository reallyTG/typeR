library(photobiology)


### Name: is.generic_spct
### Title: Query class of spectrum objects
### Aliases: is.generic_spct is.raw_spct is.calibration_spct is.cps_spct
###   is.source_spct is.response_spct is.filter_spct is.reflector_spct
###   is.object_spct is.chroma_spct is.any_spct

### ** Examples

is.source_spct(sun.spct)
is.filter_spct(sun.spct)
is.generic_spct(sun.spct)
is.generic_spct(sun.spct)

is.source_spct(sun.spct)
is.filter_spct(sun.spct)
is.generic_spct(sun.spct)
is.generic_spct(sun.spct)




