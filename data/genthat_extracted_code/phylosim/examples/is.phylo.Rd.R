library(phylosim)


### Name: is.phylo
### Title: Check if an object is an instance of the phylo class
### Aliases: is.phylo.default is.phylo

### ** Examples

	# load APE
	library(ape);
	# create some objects
	o1<-Object();
	o2<-rcoal(3);
	# check if they are phylo objects
	is.phylo(o1);
	is.phylo(o2);	

 


