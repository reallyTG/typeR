library(pipefittr)


### Name: pipefittr
### Title: Convert nested calls to magrittr's pipes.
### Aliases: pipefittr

### ** Examples

teststring = "jump_on(bop_on( scoop_up( hop_through(foo_foo, forest), field_mouse ), head))"
pipefittr(teststring, pretty = TRUE)
  




