library(pixels)


### Name: show_pixels
### Title: Show Pixels
### Aliases: show_pixels

### ** Examples


library(pixels)
show_pixels(
  round(runif(400, 0, 1)),
  grid = c(40, 10),
  size = c(800, 200),
  params = list(fill = list(color = "#FF3388"))
)




