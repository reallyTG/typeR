library(prefeR)


### Name: prefEl
### Title: A shortcut to create objects of the class BayesPrefClass.
### Aliases: prefEl

### ** Examples

p <- prefEl(data = data.frame(x = c(1,0,1), y = c(0, 1, 1)),
            priors = c(Normal(0,1), Flat()))
help(BayesPrefClass)



