library(pscl)


### Name: ntable
### Title: nicely formatted tables
### Aliases: ntable
### Keywords: print

### ** Examples

data(bioChemists)
attach(bioChemists)
ntable(fem)
ntable(fem,mar,row=TRUE)
ntable(fem,mar,per=2,col=TRUE)
ntable(fem,mar,per=2,row=TRUE,col=TRUE)



