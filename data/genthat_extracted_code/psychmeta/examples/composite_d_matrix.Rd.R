library(psychmeta)


### Name: composite_d_matrix
### Title: Matrix formula to estimate the standardized mean difference
###   associated with a weighted or unweighted composite variable
### Aliases: composite_d_matrix

### ** Examples

composite_d_matrix(d_vec = c(1, 1), r_mat = matrix(c(1, .7, .7, 1), 2, 2),
                   wt_vec = c(1, 1), p = .5)



