library(rbgm)


### Name: bgmfile
### Title: Read BGM
### Aliases: bgmfile bgmfile read_bgm

### ** Examples

library(bgmfiles)
bfile <- sample(bgmfiles(), 1L)
bgm <- bgmfile(bfile)
library(tibble)
bgm



