library(replyr)


### Name: replyr_add_ids
### Title: Add unique ids to rows.  Note: re-arranges rows in many cases.
### Aliases: replyr_add_ids

### ** Examples


replyr_add_ids(data.frame(x=c('a','b')), 'id', local_short_cut = FALSE)




