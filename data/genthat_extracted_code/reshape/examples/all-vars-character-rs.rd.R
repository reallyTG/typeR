library(reshape)


### Name: all.vars.character
### Title: Get all variables
### Aliases: all.vars.character
### Keywords: internal

### ** Examples
all.vars.character("a + b")
all.vars.character("a + b | c")
all.vars.character("a + b")
all.vars.character(". ~ a + b")
all.vars.character("a ~ b | c + d + e")


