library(rgl)


### Name: ageSetter
### Title: Set WebGL scene properties based on the age of components of
###   objects.
### Aliases: ageSetter

### ** Examples

propertySlider(ageSetter(births = 1:10, ages = c(-1, 0, 1), 
              alpha = c(0, 1, 1), objids = 123))



