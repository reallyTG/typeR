library(rowr)


### Name: len
### Title: Allows finding the 'length' without knowledge of dimensionality.
### Aliases: len

### ** Examples

len(list(1,2,3))
len(c(1,2,3,4))
df<-data.frame(a=c(1,2,3),b=c(1,2,3))
len(df)



