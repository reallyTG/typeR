library(rpf)


### Name: compressDataFrame
### Title: Compress a data frame into unique rows and frequencies
### Aliases: compressDataFrame

### ** Examples

df <- as.data.frame(matrix(c(sample.int(2, 30, replace=TRUE)), 10, 3))
compressDataFrame(df)



