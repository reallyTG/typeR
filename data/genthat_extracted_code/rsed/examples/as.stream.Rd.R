library(rsed)


### Name: as.stream
### Title: Converts a character vector to an object of class "stream"
### Aliases: as.stream print.stream

### ** Examples

aStream <- c("string1", "string2", "string3")
aStream

# Note how it prints after we convert it to a stream object:
as.stream(aStream)



