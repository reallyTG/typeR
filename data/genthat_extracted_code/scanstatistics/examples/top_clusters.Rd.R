library(scanstatistics)


### Name: top_clusters
### Title: Get the top (non-overlappig) clusters.
### Aliases: top_clusters

### ** Examples

## Not run: 
##D set.seed(1)
##D table <- data.frame(zone = 1:5, duration = 1, score = 5:1)
##D zones <- list(1:2, 1:3, 2:5, c(1, 3), 4:5, c(1, 5))
##D top_clusters(list(table = table), zones, k = 4, overlapping = FALSE)
## End(Not run)



