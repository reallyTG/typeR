library(searchable)


### Name: regex
### Title: regex
### Aliases: regex regex.SearchableOrPattern regex.character regex.default

### ** Examples

pat <- regex("a.+")
  detect( c('alpha','beta'), pat )



