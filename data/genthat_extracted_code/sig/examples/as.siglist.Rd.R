library(sig)


### Name: as.siglist
### Title: Coerce object to be a siglist
### Aliases: as.siglist as.siglist.list as.siglist.sig as.siglist.siglist

### ** Examples

as.siglist(list(
  sig(mean),
  list(name = "fun", alist(x =,y = 1))
))



