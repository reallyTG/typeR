library(soc.ca)


### Name: map.path
### Title: Map path along an ordered variable
### Aliases: map.path

### ** Examples

example(soc.ca)
map <- map.ind(result, point.color = as.numeric(sup$Age))
map <- map + scale_color_continuous(high = "red", low = "yellow")
map.path(result, sup$Age, map)



