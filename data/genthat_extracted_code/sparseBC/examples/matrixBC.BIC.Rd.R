library(sparseBC)


### Name: matrixBC.BIC
### Title: Do tuning parameter (lambda) selection for MVN biclustering via
###   BIC criterion
### Aliases: matrixBC.BIC

### ** Examples

########### Create data matrix
#x <- matrix(rnorm(20*30),nrow=20,ncol=30)

########### Perform sparseBC.BIC to select lambda
#lambda<-matrixBC.BIC(x,k=2,r=2,lambda=c(0,10,20,30,40),alpha=0.2,beta=0.2)$lambda



