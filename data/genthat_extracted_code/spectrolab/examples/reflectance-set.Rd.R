library(spectrolab)


### Name: reflectance<-
### Title: Set spectra reflectance
### Aliases: reflectance<-

### ** Examples

library(spectrolab)
spec = as.spectra(spec_matrix_example, name_idx = 1)
# scale all refletance values by 2
reflectance(spec) = reflectance(spec) * 2



