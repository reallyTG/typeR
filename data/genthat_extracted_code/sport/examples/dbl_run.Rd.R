library(sport)


### Name: dbl_run
### Title: DBL rating algorithm
### Aliases: dbl_run

### ** Examples

data <- data.frame( name = c( "A", "B", "C", "D" ), 
                    rank = c( 3, 4, 1, 2 ))
dbl <- dbl_run( rank ~ name, data)



