library(surrosurv)


### Name: simData
### Title: Generate survival times for two endpoints in a meta-analysis of
###   randomized trials
### Aliases: simData.re simData.cc simData.gh simData.mx
### Keywords: ~kwd1 ~kwd2

### ** Examples

  set.seed(1)
  simData.re(N = 2, ni = 5)
  simData.cc(N = 2, ni = 5)
  simData.mx(N = 2, ni = 5)



