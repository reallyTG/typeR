library(tswge)


### Name: est.glambda.wge
### Title: Estimate the value of lambda and offset to produce a stationary
###   dual.
### Aliases: est.glambda.wge
### Keywords: TVF g(lambda)

### ** Examples
data(ss08)
                   est.glambda.wge(ss08,lambda.range=c(-1,1),offset.range=c(0,100))



