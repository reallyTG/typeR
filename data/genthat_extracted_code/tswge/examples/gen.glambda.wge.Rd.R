library(tswge)


### Name: gen.glambda.wge
### Title: Function to generate a g(lambda) realization
### Aliases: gen.glambda.wge
### Keywords: Realization g(lambda) process TVF

### ** Examples
gen.glambda.wge(n=500, lambda=0.5,phi=c(1.9,-.99), vara=1, plot=TRUE,sn=0)


