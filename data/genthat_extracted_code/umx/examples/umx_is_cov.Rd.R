library(umx)


### Name: umx_is_cov
### Title: umx_is_cov
### Aliases: umx_is_cov

### ** Examples

df = cov(mtcars)
umx_is_cov(df)
df = cor(mtcars)
umx_is_cov(df)
umx_is_cov(df, boolean = TRUE)
umx_is_cov(mtcars, boolean = TRUE)



